# Certifications

Drop credential badge artwork here. The About page expects:

- `safe-sp.png` — SAFe® Practitioner (SP) badge (square, transparent PNG, ~256px)

If the file is missing the About page degrades gracefully to a text-only
credential (no broken image).
